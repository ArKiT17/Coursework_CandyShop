@model List<Staff>

@{
	Layout = "_Layout";
}

@foreach (Staff person in Model) {
	<div class="card">
		<img src="@person.ImagePath">
		<div class="info">
			<h3>@person.Name</h3>
			<p>@person.Description</p>
		</div>
	</div>
}